# Error Log Analyser

A simple Python project that analyzes log files for error messages and counts how many times each error occurs.

## Features
- Reads any `.log` file
- Counts occurrences of each unique error
- Displays a clean error summary

## How to Run
1. Clone this repo or download files.
2. Run the script:
   ```bash
   python analyser.py
